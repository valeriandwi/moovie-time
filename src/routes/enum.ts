export enum ROUTE_LIST {
  CATEGORIES = "CATEGORIES",
  MOVIES = "MOVIES",
  TV_SHOW = "TV_SHOW",
  LOGIN = "LOGIN",
}

export enum PATH_NAME {
  CATEGORIES = "/categories",
  MOVIES = "/movies",
  TV_SHOW = "/tv-show",
  LOGIN = "/login",
}
